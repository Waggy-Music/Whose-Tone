<%= form_with model: [post, comment], local: false do |f| %>
<%#= form_with model: [post, comment] do |f| %>
  <%= f.text_area :comment, rows: '3', placeholder: "コメントをここに" ,class:"col-md-10 mt-2"%>
  <%= f.submit "投稿", class: "btn-success" %>
  <% end %>
